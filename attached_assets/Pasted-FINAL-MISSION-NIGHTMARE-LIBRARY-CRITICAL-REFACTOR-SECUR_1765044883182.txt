FINAL MISSION: NIGHTMARE LIBRARY — CRITICAL REFACTOR, SECURITY PATCH, CLOUDFLARE CONVERSION, AND FEATURE-PRIVILEGE

Context:
This is a private EPUB reader ("Nightmare Library") being prepared for Cloudflare Pages. Replit was used as the dev environment. There is a folder named `Replit-Version/` that contains a copy of the original output — **leave that folder completely untouched** (do not modify, move, or delete its contents). All other files are in-scope.

Primary mandate (non-negotiable):
1. Follow Cloudflare Pages / Cloudflare Workers best practices and compatibility rules for every file and asset in the repo, EXCEPT the `Replit-Version/` folder which must remain exactly as-is.
2. Remove **all unsafe DOM manipulation** that introduces potential XSS risks. Replace usages of `innerHTML`, `outerHTML`, `document.write`, and template-string HTML insertions with safe DOM construction or sanitized content.
3. Fix the specific scanner findings provided (XSS locations and commented credentials). The locations to fix explicitly include (but are not limited to):
   - frontend/scripts/emoji-bookmarks.js → `panel.outerHTML = this.renderBookmarkUI(bookId);`
   - frontend/scripts/main.js → all `container.innerHTML`, `this.container.innerHTML = html`, building book cards and lists via `.join('')`
   - frontend/scripts/main.js → `elements.shelvesList.innerHTML = ...` and `elements.tagsList.innerHTML = ...`
   - frontend/scripts/main.js → `document.getElementById('shelf-options').innerHTML = ...`
   - frontend/scripts/main.js → toast HTML insertions
   - frontend/scripts/reader.js → `elements.tocList.innerHTML = renderItems(toc)` and `menu.innerHTML = ...` and `nav.innerHTML = ...`
   - frontend/scripts/reading-gamification.js → all `notification.innerHTML` / level unlock templates
   - Any other file where dynamic HTML is constructed from user or external data.
4. Replace unsafe patterns with:
   - `document.createElement()` + `.textContent` + `.appendChild()`/`.append()` or
   - Use a small safe DOM helper factory (createElementWithText(tag, text, attrs)) that centralizes safe node creation, OR
   - If HTML fragments are strictly required, sanitize with **DOMPurify** (and only if content provenance is verified).
5. Remove any hard-coded credentials or commented secrets (e.g., `wrangler.toml` commented MEGA_PASSWORD). Move secrets to environment bindings / Cloudflare secret bindings. Add comments/instructions on which bindings to create and sample env variable names (do not include actual secret values).
6. Perform an automated dependency & security audit (frontend and any build tooling). Where a package is insecure or Node-only and not Cloudflare-compatible, replace with safe alternative or sandbox it in a Cloudflare-compatible way. Do not add paid services.

Rendering & Performance Optimization (must implement):
1. Batch DOM writes with DocumentFragment and/or a small virtual DOM diffing approach for library/shelf renders.
2. Use requestAnimationFrame for any animation or repeated DOM updates.
3. Implement lazy-loading for cover images and heavy DOM lists; implement virtualization for long lists (e.g., a lightweight virtual list if the library may exceed hundreds of books).
4. Cache DOM nodes or templates for repeat renders; avoid rebuilding the entire UI on small updates.
5. Pre-render invisible components off-DOM and insert them atomically.
6. Preload and cache the next EPUB chapter(s); use Web Workers for heavy EPUB parsing or indexing tasks.
7. Use safe escapeHtml utilities for attribute/text injection and ensure it’s applied everywhere content may include tag characters.
8. Minimize paint & layout thrashing; avoid synchronous layout reads right after DOM writes.

Cloudflare Conversion & Compatibility:
1. Replace or remove Replit-specific runtime assumptions and Node-only packages in frontend code.
2. For server-like tasks (if any), port to Cloudflare Workers / Pages Functions idioms. Use Cloudflare KV / D1 / Durable Objects bindings where necessary and appropriate, but default to KV for simple key-value reads/writes (reading progress, tags, favorites).
3. Provide a `wrangler.toml` example (no secrets) and a `pages` configuration example showing env/binding names. Remove commented credentials and replace with commented placeholders that reference Cloudflare binding names.
4. Ensure any server-side code is compatible with Cloudflare Workers runtime (no direct Node fs, no child_process, use web-standard APIs).

Feature freedom:
1. You have permission to add features **if and only if** they:
   - Improve security, performance, UX, offline capability, or Cloudflare compatibility,
   - Don't require paid APIs or violate privacy (this project is local-first and private),
   - Are implementable with client-side JS, WASM, or Cloudflare-native services.
2. Prioritize local-first features: offline caching, metadata extraction, cover generation via Canvas, simple on-device summarization, improved search indexing, and safe TTS using Web Speech API.

Quality, tests, and verification (required):
1. Create a backup branch or ZIP of the repo before making sweeping changes (name backup `pre-refactor-backup-<timestamp>`).
2. Add a concise Post-Refactor Verification Checklist in the repo root (`REFRACTOR_CHECKLIST.md`) with these automated/manual checks:
   - Run UI smoke test: open main page, open reader, open shelf, toggle theme.
   - Verify no console XSS warnings remain from previously reported lines.
   - Verify all pages render without errors in a Pages preview build.
   - Verify Cloudflare Pages deploy (build) locally or in a Pages preview environment.
   - Verify secrets are only referenced via env/bindings, no passwords in files.
   - Run a simple performance measurement (Time to interactive for the main shelf with 200 mocked books) and ensure it does not regress.
   - Confirm EPUB open, next/prev chapter navigation, progress save/load works.
3. Add unit-ish tests or integration checks where feasible (small JS test scripts that load key UI components in a headless DOM like jsdom or simple run scripts).
4. Produce a `REFRACTOR_CHANGELOG.md` summarizing major changes, files touched, and rationale for security fixes.

Behavioral directives (how to do it, strictly):
1. Preserve the visual design and UX flow. Improve visuals only when doing so reduces complexity or improves accessibility (e.g., aria attributes, keyboard nav, readable contrast).
2. When converting innerHTML snippets to DOM builders, preserve original text content, semantics, and dataset attributes exactly.
3. When you make an opinionated substitution (lib choice or architecture), document the reason in `REFRACTOR_CHANGELOG.md`.
4. If a change risks breaking behavior, create a small compatibility shim and add a TODO comment for later removal.
5. Do not modify or touch `Replit-Version/` — that folder is an immutable snapshot. Any tests on the original snapshot should be read-only.

Security specifics to apply for the reported items:
- Everywhere `innerHTML` or `.outerHTML` was used, replace with safe builders or sanitize via DOMPurify. If you choose to sanitize, include the DOMPurify dependency and a small wrapper: `sanitizeHTML(input)` then `container.innerHTML = sanitizeHTML(html)`. However prefer element builders.
- Replace toast and notification templates with element factories. Example:
  ```js
  function createToast(message, options){
    const toast = document.createElement('div');
    toast.className = 'toast';
    toast.textContent = message; // never innerHTML
    if(options && options.undo) {
       const btn = document.createElement('button');
       btn.textContent = 'Undo';
       btn.onclick = options.undo;
       toast.appendChild(btn);
    }
    return toast;
  }
